<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="refresh" content="10;url=/">
    <title><%= titulo %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&family=Playfair+Display:ital,wght@0,600;1,600&display=swap');
        body { font-family: 'Poppins', sans-serif; }
        .quote-font { font-family: 'Playfair Display', serif; }
    </style>
</head>
<body class="bg-gradient-to-br from-slate-50 to-pink-50 h-screen flex items-center justify-center p-4">

    <div class="bg-white p-8 rounded-3xl shadow-2xl max-w-md w-full text-center border border-white relative overflow-hidden">
        
        <div class="absolute top-0 left-0 w-full h-2 bg-gradient-to-r from-pink-400 to-rose-500"></div>

        <% if (tipo === 'sucesso') { %>
            <div class="mx-auto w-20 h-20 bg-green-100 text-green-500 rounded-full flex items-center justify-center mb-6 shadow-inner">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                </svg>
            </div>
            <h1 class="text-3xl font-bold text-slate-800 mb-2"><%= titulo %></h1>
            <p class="text-slate-500 mb-8"><%= msg %></p>

            <% if (versiculo) { %>
                <div class="bg-pink-50 rounded-xl p-6 mb-8 relative border border-pink-100 shadow-sm">
                    <div class="absolute -top-3 -left-2 text-pink-200 text-6xl font-serif leading-none">“</div>
                    
                    <p class="quote-font text-xl text-slate-700 italic leading-relaxed relative z-10">
                        <%= versiculo.texto %>
                    </p>
                    <p class="text-xs font-bold text-pink-500 mt-3 tracking-widest uppercase">
                        — <%= versiculo.ref %>
                    </p>
                </div>
            <% } %>

        <% } else { %>
            <div class="mx-auto w-20 h-20 bg-red-100 text-red-500 rounded-full flex items-center justify-center mb-6">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </div>
            <h1 class="text-2xl font-bold text-red-500 mb-2"><%= titulo %></h1>
            <p class="text-slate-500 mb-6"><%= msg %></p>
        <% } %>

        <a href="/" class="block w-full bg-slate-800 text-white py-4 rounded-xl font-bold hover:bg-slate-900 transition shadow-lg hover:shadow-xl transform hover:-translate-y-0.5">
            Voltar ao Início
        </a>

        <div class="mt-6 flex items-center justify-center gap-2 text-xs text-slate-400">
            <svg class="animate-spin h-3 w-3 text-pink-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
            Redirecionando em <span id="timer" class="font-bold text-slate-600">10</span>s...
        </div>
    </div>

    <script>
        let segundos = 30;
        const elemento = document.getElementById('timer');
        const intervalo = setInterval(() => {
            segundos--;
            if(elemento) elemento.innerText = segundos;
            if (segundos <= 0) {
                clearInterval(intervalo);
                window.location.href = "/";
            }
        }, 1000);
    </script>
</body>
</html>